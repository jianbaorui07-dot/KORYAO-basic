[mcp_servers.starbridge]
command = "python"
args = ["-m", "starbridge_mcp.mcp_server"]

[mcp_servers.starbridge.env]
STARBRIDGE_PHOTOSHOP_SAFE_ONLY = "1"
STARBRIDGE_PHOTOSHOP_DEFAULT_DRY_RUN = "1"
STARBRIDGE_PHOTOSHOP_ALLOW_DESTRUCTIVE = "0"

# Copy this file to .codex/config.toml for this repo or merge it into
# ~/.codex/config.toml. The real .codex/config.toml is ignored by git.
# If you use the global config, set cwd to this repo's local path there;
# do not commit real workstation paths, output paths, or private project folders.
